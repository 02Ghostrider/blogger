<h1><%= @article.title %></h1>
<p><%= @article.body %></p>
<div>
	<%= link_to "Delete", article_path(@article), method: :delete, data: {confirm: "Do you really want to delete the article?"} %>
</div>
<div>
	<%= link_to "edit", edit_article_path(@article) %>
</div>
<div>
	<%= link_to "<< Back to Articles List", articles_path %>
</div>
